{
  "name": "caspian-share-functions",
  "private": true,
  "version": "0.4.0",
  "description": "Cloud Functions for Caspian Share — share-count aggregator + recordShareEvents callable.",
  "main": "lib/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "deploy": "firebase deploy --only functions"
  },
  "engines": { "node": "20" },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^6.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
